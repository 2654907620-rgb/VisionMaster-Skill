# 测量 模块参数与结果（VM 用户手册 V4.4.3）

共 10 个模块。来源：官方用户手册第 13 章对应小节。

## 13.3.1 线圆测量
*手册 PDF 551–557 页*

> 线圆测量可测量图像中直线和圆的角度、距离、垂足坐标以及交点坐标。

**参数（20）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可选择前序模块的图像数据。可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | 可选择前序模块的图像数据 |
| **来源选择** | 可选订阅或绘制两种方式 | — | 可选订阅或绘制两种方式 |
| **订阅** | 可直接订阅线和圆的数据 | — | — |
| **线输入** | 可选按线、按点、按坐标三种方式订阅直线 | — | 可选按线、按点、按坐标三种方式订阅直线 |
| **圆输入** | 可选按圆、按参数两种方式订阅圆 | — | 可选按圆、按参数两种方式订阅圆 |
| **绘制** | 绘制方式输入时，相关参数和大部分模块的操作类似，详情参见 通用模块ROI 区域的介绍 | — | — |
| **坐标系选择** | 可选图像坐标系和特定坐标系。进行计算，输出实际的物理距离数据 | — | 可选图像坐标系和特定坐标系 |
| **输出角度范围** | 设置后可执行输出符合该角度范围内的结果，可选-90°-90°或-180°-180° | — | 可选-90°-90°或-180°-180° |
| **选择类型** | 设置待测量对象的类型，可选直线1 或圆2 | — | 可选直线1 或圆2 |
| **边缘类型** | 设置待测量边缘的类型。可选项说明如下： | — | 可选项说明如下： |
| **边缘极性** | 设置待测量边缘的极性，可选项说明如下： | — | 可选项说明如下： |
| **边缘阈值** | 边缘阈值也即梯度阈值，取值范围为0~255。该参数可指定仅当图像中的边缘灰度差大于所设置的边缘阈值时，图像才能被算法检测到 | — | 取值范围为0~255 |
| **滤波尺寸** | 设置滤波器大小，可用于增强目标边缘的清晰程度并抑制噪声。该参数的最小值为1，数值越小，表示边缘越清晰且过渡带小 | — | — |
| **剔除点数** | 误差过大而被排除不参与拟合的最小点数量。一般情况下，离群点越多，该值应设置越大，可以获得更佳的查找效果 | — | — |
| **初定位** | 初定位可用于大致判定ROI 区域内更接近目标圆的区域中心，并将其作为初始圆中心，便于后续对目标圆的精细定位。该功能默认关闭，此时默认ROI 区域中心为初始圆中心。您也可以开启该功能。开启后，您还需要配置以下参数： | 默认关闭 | — |
| **下采样系数** | 指定一个下采样系数。该参数取值越大，轮廓点越稀疏，轮廓越不精细。该参数取值的上限为8 | — | — |
| **定位敏感度** | 指定一个敏感度数值，可用于排除干扰点。该参数取值越大，初定位功能排除噪音干扰的能力越强 | — | — |
| **剔除距离** | 允许离群点到直线的最大像素距离，超过该距离的点会被排除，值越小，排除点越多 | — | — |
| **初始拟合** | 局部，否则采取全局 | — | — |
| **拟合方式** | 包括最小二乘、huber 和tukey 三种。三种拟合方式只是权重的计算方式有些差异。随着离群点数量增多以及离群距离增大，可逐次使用最小二乘、huber、tukey | — | 包括最小二乘、huber 和tukey 三种 |

**输出结果（21）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **测量直线** | `line` | line 型，表示输入直线 |
| **测量直线起点** | `point` | point 型，表示输入直线的起点 |
| **测量直线起点X** | `float` | float 型，代表输入直线的起点X 坐标 |
| **测量直线起点Y** | `float` | float 型，代表输入直线的起点Y 坐标 |
| **测量直线终点** | `point` | point 型，表示输入直线的终点 |
| **测量直线终点X** | `float` | float 型，代表输入直线的终点X 坐标 |
| **测量直线终点Y** | `float` | float 型，代表输入直线的终点Y 坐标 |
| **测量直线角度** | `float` | float 型，代表输入直线相对于水平线方向的旋转角度。顺时针旋转为正，逆时针旋转为负 |
| **测量圆环** | `annull` | annull 型，代表输入或查找到的测量圆环 |
| **圆心点** | `point` | point 型，代表输入或查找到的圆心点 |
| **圆心点X** | `float` | float 型，代表输入或查找到圆的圆心点的X 坐标 |
| **圆心点Y** | `float` | float 型，代表输入或查找到圆的圆心点的Y 坐标 |
| **圆内径** | `float` | float 型，代表输入或查找到的测量圆内径。默认为0 |
| **圆半径** | `float` | float 型，代表输入或查找到的圆半径 |
| **圆起始角度** | `float` | float 型，代表输入或查找到的测量圆起始角度。默认为0 度 |
| **圆角度范围** | `float point` | float 型，代表输入或查找到的测量圆角度范围。默认为360 度。交点*（*代表1-2）point 型，代表测量直线与测量圆相交点 |
| **交点*X** | `float` | float 型，代表两个目标形状交点的X 坐标 |
| **交点*Y** | `float` | float 型，代表两个目标形状交点的Y 坐标 |
| **垂足点** | `point` | point 型，代表测量圆圆心点垂直于测量直线的交点 |
| **垂足点X** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.10 直方图工具
*手册 PDF 592–596 页*

> 直方图工具可统计目标区域内的像素个数，灰度值均值、最小值、最大值、峰值、标准差，像素数量和对比度。还可生成灰度直方图，查看各个灰度值下的像素点分布状态。

**参数（4）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 需要订阅前序模块的图像数据，可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | — |
| **输出掩膜** | 可设置是否输出掩膜图像 | — | — |
| **ROI 区域类型** |  | — | — |
| **ROI 创建** | 义或订阅相关参数 | — | — |

**输出结果（31）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **像素数量** | `int` | int 型，代表检测区域在指定阈值范围内的像素点数 |
| **最小值** | `int` | int 型，代表检测区域图像灰度值的最小值 |
| **最大值** | `int` | int 型，代表检测区域图像灰度值的最大值 |
| **中值** | `int` | int 型，代表检测区域内像素灰度值的中值 |
| **峰值** | `int` | int 型，代表检测区域内出现次数最多的像素值 |
| **均值** | `float` | float 型，代表检测区域图像灰度值的平均值 |
| **标准差** | `float` | float 型，代表检测区域图像灰度值的标准差 |
| **对比度** | `float` | float 型。使用自动二值化将区域分为较亮和较暗的两个部分，分别计算其灰度均值。两个灰度均值差即为对比度 |
| **直方图** | `int` | int 型，此为长度是256 的数组，代表在检测区域中每个灰度值所对应的像素个数 |
| **累积直方图** | `int` | int 型，代表256 定长数组，检测区域内从0 开始递增累计的像素点数 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **ROI 圆弧** | `annull` | annull 型，代表ROI 圆弧 |
| **检测圆弧中心点** | `point` | point 型，代表检测圆弧的中心点 |
| **检测圆弧中心X** | `float` | float 型，代表检测圆弧中心点的X 坐标 |
| **检测圆弧中心Y** | `float` | float 型，代表检测圆弧中心点的Y 坐标 |
| **检测圆弧内径** | `float` | float 型，代表检测圆弧的内径 |
| **检测圆弧外径** | `float` | float 型，代表检测圆弧的外径 |
| **检测圆弧起始角度** | `float` | float 型，代表检测圆弧的起始边与水平方向夹角 |
| **检测圆弧角度范围** | `float` | float 型，代表检测圆弧的起始边到终止边的角度范围 |
| **输出掩膜** | `image` | image 型，表示模块输出的掩膜图像，记录了该掩膜图像的宽度、高度和像素格式 |
| **输出掩膜图像** | `image` | image 型，代表根据检测区域和屏蔽区域计算出的掩膜图像，以二值图的形式输出 |
| **输出掩膜宽度** | `int` | int 型，代表输出掩膜图像的宽度 |
| **输出掩膜高度** | `int` | int 型，代表输出掩膜图像的高度 |
| **输出掩膜像素格式** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.2 圆圆测量
*手册 PDF 558–563 页*

> 圆圆测量可测量图像中两个圆之间的角度、距离、交点和位置关系。

**参数（18）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可选择前序模块的图像数据。可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | 可选择前序模块的图像数据 |
| **来源选择** | 可选订阅或绘制两种方式 | — | 可选订阅或绘制两种方式 |
| **订阅** | 可直接订阅2 个圆的数据 | — | — |
| **圆输入** | 可选按圆、按参数两种方式订阅圆 | — | 可选按圆、按参数两种方式订阅圆 |
| **绘制** | 绘制方式输入时，相关参数和大部分模块的操作类似，详情参见 通用模块ROI 区域的介绍 | — | — |
| **坐标系选择** | 可选图像坐标系和特定坐标系。进行计算，输出实际的物理距离数据 | — | 可选图像坐标系和特定坐标系 |
| **输出角度范围** | 设置后可执行输出符合该角度范围内的结果，可选-90°-90°或-180°-180° | — | 可选-90°-90°或-180°-180° |
| **选择圆** | 可选圆1 或圆2 | — | 可选圆1 或圆2 |
| **边缘类型** | 设置待测量边缘的类型。可选项说明如下： | — | 可选项说明如下： |
| **边缘极性** | 设置待测量边缘的极性，可选项说明如下： | — | 可选项说明如下： |
| **滤波尺寸** | 设置滤波器大小，可用于增强目标边缘的清晰程度并抑制噪声。该参数的最小值为1，数值越小，表示边缘越清晰且过渡带小 | — | — |
| **剔除点数** | 误差过大而被排除不参与拟合的最小点数量。一般情况下，离群点越多，该值应设置越大，可以获得更佳的查找效果 | — | — |
| **初定位** | 初定位可用于大致判定ROI 区域内更接近目标圆的区域中心，并将其作为初始圆中心，便于后续对目标圆的精细定位。一般情况下，圆查找前序模块为位置修正，建议关闭初定位。关闭该功能后，默认将ROI 区域中心作为初始圆中心。若您选择开启该功能，则需要配置以下参数： | — | — |
| **下采样系数** | 指定一个下采样系数。该参数取值越大，轮廓点越稀疏，轮廓越不精细。该参数取值的上限为8 | — | — |
| **定位敏感度** | 指定一个敏感度数值，可用于排除干扰点。该参数取值越大，初定位功能排除噪音干扰的能力越强 | — | — |
| **剔除距离** | 允许离群点到圆心的最大像素距离，超过该距离的点会被排除，值越小，排除点越多 | — | — |
| **初始拟合** | 部，否则采取全局 | — | — |
| **拟合方式** | 包括最小二乘、huber 和tukey 三种。三种拟合方式只是权重的计算方式有些差异。随着离群点数量增多以及离群距离增大，可逐次使用最小二乘、huber、tukey | — | 包括最小二乘、huber 和tukey 三种 |

**输出结果（13）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色。测量圆环*（*代表1-2） |
| **测量圆心*** | `—` |  |
| **圆*圆心X** | `float` | float 型，代表输入或查找到的圆的圆心点的X 坐标 |
| **圆*圆心Y** | `float` | float 型，代表输入或查找到的圆的圆心点的Y 坐标 |
| **圆*内径** | `float` | float 型，代表输入或查找到的测量圆内径（默认为0） |
| **圆*半径** | `float` | float 型，代表输入或查找到的圆半径 |
| **圆*起始角度** | `float` | float 型，代表输入或查找到的测量圆起始角度（默认为0 度） |
| **圆*角度范围** | `float point` | float 型，代表输入或查找到的测量圆角度范围（默认为360 度）。交点*（*代表1-2）point 型，代表测量直线与测量圆相交点 |
| **交点*X** | `float` | float 型，代表两个目标形状交点的X 坐标 |
| **交点*Y** | `float` | float 型，代表两个目标形状交点的Y 坐标 |
| **角度** | `float` | float 型，代表最小外接矩形的较长边基于水平线旋转的角度。其中，顺时针旋转为正角度，逆时针旋转为负角度 |
| **距离** | `float` | float 型，代表测量圆的圆心垂直于测量直线的距离 |
| **位置关系** | `string` | string 型，Inside 代表内含，Inscribe 代表内切，Intersect 代表相交，Circumscribe 代表外切，Outside 代表外离 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.3 点圆测量
*手册 PDF 564–567 页*

> 点圆测量可测量图像中点与圆之间的角度、中心距离、最近距离和最远距离。

**参数（5）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可选择前序模块的图像数据 | — | 可选择前序模块的图像数据 |
| **点输入** | 可选按点、按坐标两种方式订阅点 | — | 可选按点、按坐标两种方式订阅点 |
| **圆输入** | 可选按圆、按参数两种方式订阅圆 | — | 可选按圆、按参数两种方式订阅圆 |
| **坐标系选择** | 可选图像坐标系和特定坐标系。进行计算，输出实际的物理距离数据 | — | 可选图像坐标系和特定坐标系 |
| **输出角度范围** | 设置后可执行输出符合该角度范围内的结果，可选-90°-90°或-180°-180° | — | 可选-90°-90°或-180°-180° |

**输出结果（16）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **测量点** | `point` | point 型，表示输入点 |
| **测量点X** | `float` | float 型，代表输入点的X 坐标 |
| **测量点Y** | `float` | float 型，代表输入点的Y 坐标 |
| **测量圆环** | `annull` | annull 型，代表输入或查找到的测量圆环 |
| **圆心点** | `point` | point 型，代表输入或查找到的圆心点 |
| **圆心点X** | `float` | float 型，代表输入或查找到圆的圆心点的X 坐标 |
| **圆心点Y** | `float` | float 型，代表输入或查找到圆的圆心点的Y 坐标 |
| **圆内径** | `float` | float 型，代表输入或查找到的测量圆内径。默认为0 |
| **圆半径** | `float` | float 型，代表输入或查找到的圆半径 |
| **圆起始角度** | `float` | float 型，代表输入或查找到的测量圆起始角度。默认为0 度 |
| **圆角度范围** | `float` | float 型，代表输入或查找到的测量圆角度范围。默认为360 度 |
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **角度** | `float` | float 型，代表最小外接矩形的较长边基于水平线旋转的角度。其中，顺时针旋转为正角度，逆时针旋转为负角度 |
| **中心距离** | `float` | float 型，代表输入测量点到输入测量圆圆心的距离 |
| **最近距离** | `float` | float 型，代表输入测量点到输入测量圆上最近的距离 |
| **最远距离** | `float` | float 型，代表输入测量点到输入测量圆上最远的距离 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.4 点线测量
*手册 PDF 568–570 页*

> 点线测量可测量图像中点与线之间的垂线角度、垂直距离、最近距离、最远距离和垂足点坐标。

**参数（5）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可选择前序模块的图像数据 | — | 可选择前序模块的图像数据 |
| **点输入** | 可选按点、按坐标两种方式订阅点 | — | 可选按点、按坐标两种方式订阅点 |
| **线输入** | 可选按线、按点、按坐标三种方式订阅直线 | — | 可选按线、按点、按坐标三种方式订阅直线 |
| **坐标系选择** | 可选图像坐标系和特定坐标系。进行计算，输出实际的物理距离数据 | — | 可选图像坐标系和特定坐标系 |
| **输出角度范围** | 设置后可执行输出符合该角度范围内的结果，可选-90°-90°或-180°-180° | — | 可选-90°-90°或-180°-180° |

**输出结果（10）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **测量点** | `point` | point 型，表示输入点 |
| **测量点X** | `float` | float 型，代表输入点的X 坐标 |
| **测量点Y** | `float` | float 型，代表输入点的Y 坐标 |
| **测量直线** | `line` | line 型，表示输入直线 |
| **测量直线起点** | `point` | point 型，表示输入直线的起点 |
| **测量直线起点X** | `float` | float 型，代表输入直线的起点X 坐标 |
| **测量直线起点Y** | `float` | float 型，代表输入直线的起点Y 坐标 |
| **测量直线终点** | `point` | point 型，表示输入直线的终点 |
| **测量直线终点X** | `float` | float 型，代表输入直线的终点X 坐标 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.5 线线测量
*手册 PDF 571–576 页*

> 线线测量可测量图像中两条直线之间的夹角、绝对距离和交点坐标。

**参数（16）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可选择前序模块的图像数据。可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | 可选择前序模块的图像数据 |
| **来源选择** | 可选订阅或绘制两种方式 | — | 可选订阅或绘制两种方式 |
| **订阅** | 可直接订阅2 条线的数据 | — | — |
| **线输入** | 可选按线、按点、按坐标三种方式订阅直线 | — | 可选按线、按点、按坐标三种方式订阅直线 |
| **绘制** | 绘制方式输入时，您可以选择使用直线卡尺1 和直线卡尺2 分别绘制待测量线段，并选择是否开启位置修正功能。更多有关直线卡尺和位置修正的操作说明，请参见 通用模块ROI 区域 | — | — |
| **坐标系选择** | 可选图像坐标系和特定坐标系。进行计算，输出实际的物理距离数据 | — | 可选图像坐标系和特定坐标系 |
| **选择直线** | 可选直线1 或直线2 | — | 可选直线1 或直线2 |
| **边缘类型** |  | — | — |
| **边缘极性** |  | — | — |
| **边缘阈值** | 只有当图像中的边缘灰度差大于所设置的边缘阈值才能被检测出。数值越大，抗噪能力越强，得到的边缘数量越少 | — | — |
| **滤波尺寸** | 描述目标边缘的清晰程度，最小值为1，值越小，表示边缘越清晰且过渡带小。当边缘模糊或有噪声干扰时，增大该值有利于使得检测结果更加稳定，如果边缘之间距离小于滤波尺寸，会影响边缘位置的精度 | — | — |
| **剔除点数** | 误差过大而被排除不参与拟合的最小点数量。一般情况下，离群点越多，该值应设置越大，可以获得更佳的查找效果 | — | — |
| **剔除距离** | 允许离群点到直线的最大像素距离，超过该距离的点会被排除，值越小，排除点越多 | — | — |
| **初始拟合** | 局部，否则采取全局 | — | — |
| **拟合方式** | 包括最小二乘、huber 和tukey 三种。三种拟合方式只是权重的计算方式有些差异。随着离群点数量增多以及离群距离增大，可逐次使用最小二乘、huber、tukey | — | 包括最小二乘、huber 和tukey 三种 |
| **输出角度范围** | 设置后可执行输出符合该角度范围内的结果，可选-90°-90°或-180°-180° | — | 可选-90°-90°或-180°-180° |

**输出结果（13）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int line` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色。测量直线*（*代表1-2）line 型，代表用于查找中线的输入直线，记录了有关该直线的起点和终点坐标 |
| **测量直线*起点** | `point` | point 型，代表输出直线的起点 |
| **测量直线*起点X** | `float` | float 型，代表输入直线起点的X 坐标 |
| **测量直线*起点Y** | `float` | float 型，代表输入直线起点的Y 坐标 |
| **测量直线*终点** | `point` | point 型，代表输出直线的终点 |
| **测量直线*终点X** | `float` | float 型，代表输入直线终点的X 坐标 |
| **测量直线*终点Y** | `float` | float 型，代表输入直线终点的Y 坐标。测量直线*角度（*代表1-2）float 型，代表输入直线相对于水平轴的旋转角度。顺时针旋转为正，逆时针旋转为负 |
| **交点** | `point` | point 型，代表两条测量直线的交点 |
| **交点X** | `float` | float 型，代表输入直线1 与输入直线2 交点的X 坐标 |
| **交点Y** | `float` | float 型，代表输入直线1 与输入直线2 交点的Y 坐标 |
| **夹角** | `float` | float 型，代表输入直线1 与输入直线2 相交的锐角大小 |
| **绝对距离** | `float point` | float 型，代表两条直线的起点和终点（共4 个点）到对应另一线直线垂直距离的平均值。角度标识点*（*代表1-5）point 型，代表角度标识点 |
| **角度标识点*X** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.6 点点测量
*手册 PDF 577–579 页*

> 点点测量可测量图像中两点之间的角度、距离和中点坐标。

**参数（5）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可选择前序模块的图像数据 | — | 可选择前序模块的图像数据 |
| **起点输入** |  | — | — |
| **终点输入** | 可选按点、按坐标两种方式订阅点 | — | 可选按点、按坐标两种方式订阅点 |
| **坐标系选择** | 可选图像坐标系和特定坐标系。进行计算，输出实际的物理距离数据 | — | 可选图像坐标系和特定坐标系 |
| **输出角度范围** | 设置后可执行输出符合该角度范围内的结果，可选-90°-90°或-180°-180° | — | 可选-90°-90°或-180°-180° |

**输出结果（1）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色。测量点*（*代表1-2） |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.7 亮度测量
*手册 PDF 580–583 页*

> 亮度测量可测量图像的ROI区域内所有像素点的灰度均值和灰度标准差。

**参数（4）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 需要订阅前序模块的图像数据，可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | — |
| **输出掩膜** | 可设置是否输出掩膜图像 | — | — |
| **ROI 区域类型** |  | — | — |
| **ROI 创建** | 义或订阅相关参数 | — | — |

**输出结果（15）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **最小值** | `int` | int 型，代表检测区域图像灰度值的最小值 |
| **最大值** | `int` | int 型，代表检测区域图像灰度值的最大值 |
| **均值** | `float` | float 型，代表检测区域图像灰度值的平均值 |
| **标准差** | `float` | float 型，代表检测区域图像灰度值的标准差 |
| **直方图** | `int` | int 型，此为长度是256 的数组，代表在检测区域中每个灰度值所对应的像素个数 |
| **对比度** | `float` | float 型。使用自动二值化将区域分为较亮和较暗的两个部分，分别计算其灰度均值。两个灰度均值差即为对比度 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **ROI 圆弧** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.8 间距检测
*手册 PDF 584–588 页*

> 间距检测可检测目标区域内满足条件的边缘间距。

**参数（10）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 需要订阅前序模块的图像数据，可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | — |
| **ROI 创建** | 坐标和宽高度。运行参数如下： | — | — |
| **滤波尺寸** | 描述目标边缘的清晰程度，最小值为1，值越小，表示边缘越清晰且过渡带小。当边缘模糊或有噪声干扰时，增大该值有利于使得检测结果更加稳定，如果边缘之间距离小于滤波尺寸，会影响边缘位置的精度 | — | — |
| **边缘阈值** | 只有当图像中的边缘灰度差大于所设置的边缘阈值才能被检测出。数值越大，抗噪能力越强，得到的边缘数量越少 | — | — |
| **边缘0/1 极性** |  | — | — |
| **最大结果数** | 最大的查找的数量 | — | — |
| **排序方式** | 可根据需求按照分数或方向维度排序。支持按照分数升序、分数降序、方向正序和方向逆序排序 | — | 支持按照分数升序、分数降序、方向正序和方向逆序排序 |
| **边缘对类型** |  | — | — |
| **查找方向** | 指定检测的查找方向。可选择四边形从上到下或扇环形由内向外搜索或四边形从左到右或扇环形逆时针搜索 | — | 可选择四边形从上到下或扇环形由内向外搜索或四边形从左到右或扇环形逆时针搜索 |
| **最小边缘分数** | 查找到边缘的最小得分，得分低于最小分数的边缘会被过滤 | — | — |

**输出结果（17）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **边缘状态** | `int` | int 型，代表图像上某个点是否位于边缘。1 代表位于边缘，0 代表不在边缘 |
| **边缘个数** | `int` | int 型，代表查找到的边缘个数 |
| **测量宽度** | `float` | float 型，代表检测到间距的宽度 |
| **分数** | `float int` | float 型，代表匹配到图像的得分，1 表示与模板图像完全相同。边缘*极性（*代表0-1）int 型，代表边缘的灰度变化。1 表示从黑到白，2 表示从白到黑，3 表 示任意极性。边缘*位置（*代表0-1）float 型，代表边缘点所在的位置。边缘点*（*代表0-1） |
| **边缘点*X** | `float` | float 型，代表检测到的第（*+1）个边缘点的X 坐标 |
| **边缘点*Y** | `float` | float 型，代表检测到的第（*+1）个边缘点的Y 坐标。直线*起点（*代表0-1） |
| **直线*起点X** | `float` | float 型，代表边缘直线*起点的X 坐标 |
| **直线*起点Y** | `float` | float 型，代表边缘直线*起点的Y 坐标。直线*终点（*代表0-1） |
| **直线*终点X** | `float` | float 型，代表边缘直线*终点的X 坐标 |
| **直线*终点Y** | `float` | float 型，代表边缘直线*终点的Y 坐标。直线*角度（*代表0-1）float 型，代表边缘直线相对于水平线方向的旋转角度。顺时针旋转，则为正角度；逆时针旋转，则为负角度 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.3.9 像素统计
*手册 PDF 589–591 页*

> 像素统计可统计ROI区域内满足高低阈值灰度设置的像素点个数。（自4.3.0版本起弃用）

**参数（5）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 需要订阅前序模块的图像数据，可以是图像源模块输出的图像数据，也可以是图像处理模块处理后的图像数据 | — | — |
| **输出掩膜** | 可设置是否输出掩膜图像 | — | — |
| **ROI 区域类型** |  | — | — |
| **ROI 创建** | 义或订阅相关参数 | — | — |
| **阈值范围** | 需统计区域中的像素灰度值范围，可设置多个阈值范围 | — | — |

**输出结果（16）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **像素数量** | `int` | int 型，代表检测区域在指定阈值范围内的像素点数 |
| **比率** | `float` | float 型，代表检测区域内在指定阈值范围内的像素点数与总的像素点数的比值 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **ROI 圆弧** | `annull` | annull 型，代表ROI 圆弧 |
| **检测圆弧中心点** | `point` | point 型，代表检测圆弧的中心点 |
| **检测圆弧中心X** | `float` | float 型，代表检测圆弧中心点的X 坐标 |
| **检测圆弧中心Y** | `float` | float 型，代表检测圆弧中心点的Y 坐标 |
| **检测圆弧内径** | `float` | float 型，代表检测圆弧的内径 |
| **检测圆弧外径** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)
