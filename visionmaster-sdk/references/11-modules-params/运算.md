# 运算 模块参数与结果（VM 用户手册 V4.4.3）

共 11 个模块。来源：官方用户手册第 13 章对应小节。

## 13.9.1 单点对位
*手册 PDF 907–908 页*

> 单点对位可根据输入的目标点位置和方向、对象点位置和方向，计算出由对象点对位至目标点需要的移动量。

**参数（5）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入方式** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **示教点0-运行点0** | 设置运行点对位至示教点的点坐标及角度，可自定义输入或订阅前序模块对应的数据。一般从前序模块订阅 | — | — |
| **点** |  | — | — |
| **点X/Y** | 设置点坐标的对位关系 | — | — |
| **角度** | 设置角度的对位关系 | — | — |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.10 变量计算
*手册 PDF 928–930 页*

> 变量计算支持多个混合运算，可自定义参数也可以选择数据进行计算。

**参数（16）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **点击** | 可添加变量的计算表达式，默认已添加一个，支持添加多个。通过重置按钮，还可将所有变量重置为初始值。每个表达式由以下参数组成：可在表达式窗口对具体公式进行配置。具体参见 表达式窗口介绍 。以及点和常数相乘，不支持设置初始化及初始值，其他表达式窗口不支持的相关功能均为置灰状态。轮运算结果输入到公式中参与运算。：可删除该变量的计算表达式。表达式窗口介绍表达式配置窗口如下图所示，相关介绍如下：可订阅前序模块。做介绍，具体参见下表，其他不详细展开 | — | 支持添加多个 ; 支持设置初始化及初始值 ; 支持的相关功能均为置灰状态 |
| **函数** |  | — | — |
| **函数** |  | — | — |
| **返回x 与y 中的较大值** | min(x,y) | — | — |
| **返回x 与y 中的较小值** |  | — | — |
| **函数** |  | — | — |
| **函数** |  | — | — |
| **数** | trunc(x) | — | — |
| **将x 的小数位取0 后返回整数** | ceil(x) | — | — |
| **返回大于等于x 的最小整数值** | floor(x) | — | — |
| **返回小于等于x 的最大整数** | log(x) | — | — |
| **返回指定数字的自然对数（底为** | e）exp(x) | — | — |
| **返回e 的x 次幂** | pow(x,y) | — | — |
| **数字 x 的 y 次幂** | log10(x) 返回x 以10 为底的对数sqrt(x) | — | — |
| **返回x 的平方根** | abs(x) | — | — |
| **返回x 的绝对值** |  | — | — |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.11 坐标转换
*手册 PDF 931–932 页*

> 坐标转换可将当前图像上某个点的坐标通过相关计算转换其他坐标点信息。

**参数（11）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **划片信息** | 可设置是否开启坐标转换使能，并设置相关参数 | — | — |
| **坐标转换使能** | 该功能默认开启。若关闭，则输入点和输出点的数据完全一致 | 默认开启 | — |
| **输入方式** | 可设置划片中心点的输入方式，可选按点、按坐标 | — | 可选按点、按坐标 |
| **划片中心点** |  | — | — |
| **划片中心点X/Y** | 一般从前序划片拆分模块订阅划片中心点即可 | — | — |
| **划片宽** |  | — | — |
| **划片高** | 一般从前序划片拆分模块订阅划片区域的宽/高即可 | — | — |
| **输入点** | 设置输入点坐标的信息来源 | — | — |
| **输入方式** | 可选按点、按坐标 | — | 可选按点、按坐标 |
| **点** |  | — | — |
| **点X/Y** |  | — | — |

**输出结果（1）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色。13.9.11 坐标转换该模块可将图像上某个点的坐标通过计算转换为其他坐标点信息。一般配合划片拆分模块使用，将划片中的某点坐标转到原图中（即划片前的图像），可获取各个划片区域点在原图中的坐标信息。本节内容包含：该模块通过获取到的划片区域在原图中的划片中心点坐标、宽、高，从而计算出划片区域左上角在原图中的坐标P，再将需要转换点的坐标加上坐标P，即可得到转换点在原图中的坐标信息 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.2 单点抓取
*手册 PDF 909–911 页*

> 单点抓取模块基于图像基准点与图像运行点坐标实现抓取，输出相对坐标以及绝对坐标。

**参数（12）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入方式** | 可对标定类型和输入方式参数进行设置 | — | — |
| **标定类型** | 可选平移标定、平移旋转标定，根据实际需求选择 | — | 可选平移标定、平移旋转标定 |
| **输入方式** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **像素点** | 通过点（或坐标X/Y）及角度设置图像的基准点和运行点。完成像素点订阅后，点击执行再点击创建基准，若提示“基准点创建成功”，则完成基准点的设置。后续订阅的像素点均为图像运行点 | — | — |
| **示教物理点** | 通过点（或坐标X/Y）及角度设置物料在基准位置时运动机构抓取物料的绝对物理坐标及角度 | — | — |
| **示教拍照物理点** | 通过点（或坐标X/Y）及角度设置物料在基准位置时，运动机构携带相机拍照的绝对物理坐标及角度，一般为第5 个点拍照位的物理坐标。若生产时，相机相对标定时角度有所变化，还需开启旋转拍照使能并设置旋转相对角度。角度可根据实际情况自定义填写或从外部通信设备订阅 | — | — |
| **标定文件** | 可对加载及刷新标定文件进行相关设置 | — | — |
| **加载方式** | 可设置标定文件的加载方式，可选标定矩阵或加载标定文件 | — | 可选标定矩阵或加载标定文件 |
| **标定矩阵** | 通过标定矩阵进行订阅 | — | — |
| **加载标定文件** |  | — | — |
| **点击** | 可选择需加载的标定文件 | — | 可选择需加载的标定文件 |
| **刷新信号** | 可自定义或订阅数据，作为标定文件是否刷新的信号。的标定文件是最新的 | — | — |

**输出结果（6）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **x 偏移** | `float` | float 型，代表示教点到运行点X 方向的位置移动量 |
| **y 偏移** | `float` | float 型，代表示教点到运行点Y 方向的位置移动量 |
| **theta 偏移** | `float` | float 型，代表示教点到运行点的角度偏移量，顺时针为正。13.9.2 单点抓取该模块在单相机拍物料的抓取场景中使用，通过该模块可得到基准位移动到运行位的相对偏移量和绝对抓取坐标。本节内容包含：该模块需与 平移旋转标定 模块配合使用，相机和运动机构通过标定得到坐标系转换关系（即标定矩阵M）。单点抓取再根据订阅的标定矩阵将图像特征点转换到运动机构的物理坐标系下，从而推算出抓取坐标，用于引导运动机构抓取物料。特征点像素坐标定义为p1，再将运动机构移动至示教物理位P2，使得末端夹具刚好对准特征点中心。实际生产时，将运动机构运动至示教拍照位P1 时拍摄的特征点像素坐标定义为p2。先将像素坐标p1、p2 从图像坐标系转换到运动机构的物理坐标系下，再计算转换后坐标的差值（即相对偏移量）即可。其中相对偏移量与示教物理位之和，就是绝对抓取坐标。会导致末端夹具的X/Y 坐标发生偏移。为补偿不共轴旋转引起的 |
| **基准点** | `point` | point 型，代表图像提取到的特征点 |
| **基准点X** | `float` | float 型，代表图像提取到的特征点X |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.3 单点映射对位
*手册 PDF 912–914 页*

> 单点映射对位模块基于对象图像特征点与目标图像特征点坐标实现上下相机对位，输出相对坐标以及绝对坐标。

**参数（14）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入方式** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **对象像素点** | 通过点（或坐标X/Y）及角度设置对象位（一般指下相机拍照位）的图像像素点及角度 | — | — |
| **目标像素点** | 通过点（或坐标X/Y）设置目标位（一般指上相机拍照位）的图像像素点 | — | — |
| **目标像素直线起点** |  | — | — |
| **目标像素直线终点** | 通过点（或坐标X/Y）设置目标位对位直线的起点/终点。映射到下相机坐标系，确保角度统一 | — | — |
| **示教物理点** | 通过点（或坐标X/Y）及角度设置相机在做上下相机映射标定时，运动机构抓取标定板时的绝对物理坐标 | — | — |
| **标定文件** | 可对加载及刷新标定文件进行相关设置 | — | — |
| **加载方式** | 可设置标定文件的加载方式，可选标定矩阵或加载标定文件 | — | 可选标定矩阵或加载标定文件 |
| **N 点标定矩阵** |  | — | — |
| **映射标定矩阵** | 可分别通过订阅N 点标定或映射标定的矩阵 | — | — |
| **N 点标定文件** |  | — | — |
| **映射标定文件** |  | — | — |
| **点击** | 可分别选择需加载的标定文件 | — | — |
| **刷新信号** | 可自定义或订阅数据，作为标定文件是否刷新的信号。的标定文件是最新的 | — | — |

**输出结果（4）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **相对坐标X** | `float` | float 型，代表机械臂抓取物料的相对基准位置变化的偏移量X 坐标 |
| **相对坐标Y** | `float` | float 型，代表机械臂抓取物料的相对基准位置变化的偏移量Y 坐标 |
| **相对坐标R** | `float` | float 型，代表机械臂抓取物料的相对坐标角度偏移量 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.4 单点纠偏
*手册 PDF 915–916 页*

> 单点纠偏模块基于图像基准点与图像运行点坐标实现纠偏，输出相对坐标以及绝对坐标。

**输出结果（6）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **基准点** | `point` | point 型，代表图像提取到的特征点 |
| **基准点X** | `float` | float 型，代表图像提取到的特征点X |
| **基准点Y** | `float` | float 型，代表图像提取到的特征点Y |
| **基准点R** | `float` | float 型，代表图像提取到的特征点角度 |
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **相对坐标X** | `float` | float 型，代表机械臂抓取物料的相对基准位置变化的偏移量X 坐标 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.5 标定转换
*手册 PDF 917–919 页*

> 完成标定后，可通过标定转换实现相机坐标系和机械臂世界坐标系之间的转换。

**参数（7）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **坐标点输入** | 需设置坐标点的相关信息，并选择坐标类型 | — | — |
| **输入方式** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **坐标点** |  | — | — |
| **坐标X/Y** | 需转换的特征点坐标信息，可从前前序模块订阅，也可自定义 | — | — |
| **角度** | 需转换的特征点角度 | — | — |
| **坐标类型** | 可选图像坐标或物理坐标。定义坐标点输入处设置的坐标点为图像坐标，还是物理坐标 | — | 可选图像坐标或物理坐标 |
| **标定文件** | 与单点抓取模块中对应参数基本一致，此处不再赘述，具体参见 单点抓取 的同名参数 | — | — |

**输出结果（12）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **输出点** | `point` | point 型，代表对输入坐标通过标定转换/逆转换后得到的的点 |
| **转换坐标X** | `float` | float 型，代表对输入坐标通过标定转换/逆转换后得到的X 坐标 |
| **转换坐标Y** | `float` | float 型，代表对输入坐标通过标定转换/逆转换后得到的Y 坐标 |
| **转换角度** | `float` | float 型，代表对输入角度通过标定转换/逆转换后得到的角度 |
| **单像素精度** | `float` | float 型，代表单个像素对应物理坐标系下的尺寸 |
| **平移X** | `float` | float 型，代表利用计算得到的标定矩阵，将世界坐标系原点映射到图像坐标系得到的坐标X |
| **平移Y** | `float` | float 型，代表利用计算得到的标定矩阵，将世界坐标系原点映射到图像坐标系得到的坐标Y |
| **旋转** | `float` | float 型，代表机构物理坐标系相对于图像坐标系的旋转角度（单位为弧度）。系X 轴方向一致；系X 轴方向一致 |
| **尺度** | `float` | float 型，代表世界坐标系中单位长度对应图像坐标系中的像素数 |
| **斜切** | `float` | float 型，代表世界坐标系的Y 轴旋转角度与X 轴旋转角度之差（单位为弧度） |
| **宽高比** | `float` | float 型，代表世界坐标系的Y 轴缩放量与X 轴缩放量的比例 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.6 点集对位
*手册 PDF 920–920 页*

> 点集对位可根据输入的目标点集的x数组和y数组、对象点集的x数组和y数组，计算出由点对位至目标点需要的移动量。

**参数（2）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入方式** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **示教点-运行点** |  | — | — |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.7 旋转计算
*手册 PDF 921–922 页*

> 旋转计算可将点或线绕着旋转中心点按照旋转角度逆时针旋转，并得到旋转之后该点或线的相关信息。

**参数（5）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入类型** | 通过数据类型设置旋转计算的目标，可选按点、按线 | — | 可选按点、按线 |
| **点输入** | 选择旋转点时，设置旋转的点即可 | — | — |
| **线输入** | 选择旋转线时，设置旋转的线即可 | — | — |
| **旋转中心坐标** | 可设置旋转时的中心点 | — | — |
| **旋转角度** | 可设置基于旋转中心点的旋转角度。正为顺时针旋转，负为逆时针旋转 | — | — |

**输出结果（4）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **x 偏移** | `float` | float 型，代表示教点到运行点X 方向的位置移动量 |
| **y 偏移** | `float` | float 型，代表示教点到运行点Y 方向的位置移动量 |
| **theta 偏移** | `float` | float 型，代表示教点到运行点的角度偏移量，顺时针为正。13.9.7 旋转计算该模块可将点或线绕着旋转中心点按照旋转角度旋转，并计算得到旋转之后该点或线的相关信息。本节内容包含：该模块的原理为下图所示公式。其中Out X/Y 为输出点的坐标，In X/Y 为输入点的坐标，X0和Y0 为旋转中心的坐标，θ 为图像坐标系下旋转角度（顺时针为正，逆时针为负） |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.8 线对位
*手册 PDF 923–924 页*

> 线对位可根据输入的目标线集以及对象线集的直线数组，计算出由“对象线”对位至“目标线”需要的移动量。

**输出结果（12）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **输出点** | `point` | point 型，代表输入点旋转后得到的点 |
| **输出点X** | `float` | float 型，代表输入点旋转后得到点的X 坐标 |
| **输出点Y** | `float` | float 型，代表输入点旋转后得到点的Y 坐标 |
| **输出线** | `—` |  |
| **线起点** | `—` |  |
| **线起点X** | `float` | float 型，代表输入的线，旋转后得到线起点的X 坐标 |
| **线起点Y** | `float` | float 型，代表输入的线，旋转后得到线起点的Y 坐标 |
| **线终点** | `—` |  |
| **线终点X** | `float` | float 型，代表输入的线，旋转后得到线终点的X 坐标 |
| **线终点Y** | `float` | float 型，代表输入的线，旋转后得到线终点的Y 坐标 。13.9.8 线对位该模块的原理和功能与点集对位模块类似，差别点仅在于点集对位模块针对的是点，而该模块针对的是线。本节内容包含： |
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.9.9 单位转换
*手册 PDF 925–927 页*

> 单位转换可转换距离、宽度等像素单位到物理单位。

**参数（6）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **单位转换** | 通过像素间距订阅前序模块输出的距离 | — | — |
| **标定文件** | 需设置加载标定文件、刷新信号和像素当量修正参数 | — | — |
| **加载标定文件** |  | — | — |
| **点击** | 可选择需加载的标定文件。支持iwcal、txt 和xml 格式的标定文件 | — | 可选择需加载的标定文件 ; 支持iwcal、txt |
| **刷新信号** | 该参数为必填项，默认设置为0。通过自定义或订阅的参数值，您可以决定是否刷新标定文件以及像素当量修正参数设置是否生效。不同参数值对应的生效规则说明如下。像素当量修正参数设置不生效。的标定文件是最新的，且像素当量修正参数设置生效 | — | — |
| **像素当量修正** | 该参数为必填项，默认设置为1。该参数可用于微调订阅的像素间距。设置完成后，模块将结合指定的修正系数，输出校正后的物理距离。单位转换的计算公式如下：输出的物理距离=像素间距*单像素精度*像素当量修正 | — | — |

**输出结果（6）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **单像素精度** | `float` | float 型，代表单个像素对应物理坐标系下的尺寸 |
| **转换结果** | `float` | float 型，代表对输入角度通过单位转换后得到的结果 |
| **像素距离** | `float` | float 型，代表像素对应物理坐标系下的距离 |
| **平移X** | `float` | float 型，代表利用计算得到的标定矩阵，将世界坐标系原点映射到图像坐标系得到的坐标X |
| **平移Y** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)
