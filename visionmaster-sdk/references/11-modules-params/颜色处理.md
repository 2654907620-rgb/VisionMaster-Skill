# 颜色处理 模块参数与结果（VM 用户手册 V4.4.3）

共 6 个模块。来源：官方用户手册第 13 章对应小节。

## 13.11.1 颜色抽取
*手册 PDF 1047–1050 页*

> 颜色抽取可将目标区域从彩色图像中分割出来，最终得到只包含目标物体的二值图。

**参数（6）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **抽取列表** | 显示当前已设置的颜色范围，点击一个颜色范围后可编辑相应的运行参数。通过点击加号，可以设置多个颜色范围，所有范围取并集作为目标的颜色范围 | — | — |
| **颜色空间** | 支持RGB、HSV、HSI 三种颜色空间，具体介绍请见 术语介绍 章节 | — | 支持RGB、HSV、HSI |
| **通道一/二/三** | 当颜色空间选择RGB 颜色空间时，通道一、二、三分别对应R、G、B 三通道；当选择HSV 颜色空间时，通道一、二、三分别对应H、S、V 通道；当选择HSI 颜色空间时，通道一、二、三分别对应H、S、I 通道。当颜色空间分别选择RGB/HSV/HSI 颜色空间，通道一/二/三的上下限值为默认的0 和255 时，抽取列表中的颜色范围示例如下所示 | — | — |
| **通道下限** | 指定颜色空间内，用来存储、传递与颜色相关的图像信息的通道抽取像素的最小值 | — | — |
| **通道上限** | 指定颜色空间内，用来存储、传递与颜色相关的图像信息的通道抽取像素的最大值 | — | — |
| **颜色反转** | 若开启颜色反转使能，则将当前颜色空间中设置的颜色范围的补集作为实际抽取范围 | — | — |

**输出结果（20）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **总面积** | `float` | float 型，代表所抽取颜色在输入图像中所占据的总面积，也就是输出图像中白色区域的面积 |
| **输出图像** | `—` |  |
| **输出图像数据** | `binary` | binary 型，代表输出图像的二进制数据 |
| **输出图像宽度** | `int` | int 型，代表输出图像的宽度 |
| **输出图像高度** | `int` | int 型，代表输出图像的高度 |
| **输出图像像素格式** | `int` | int 型，代表输出图像的像素格式。其中17301505 对应Mono8 格式，35127316 对应RGB24 格式 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **ROI 圆弧** | `annull` | annull 型，代表ROI 圆弧 |
| **检测圆弧中心点** | `point` | point 型，代表检测圆弧的中心点 |
| **检测圆弧中心X** | `float` | float 型，代表检测圆弧中心点的X 坐标 |
| **检测圆弧中心Y** | `float` | float 型，代表检测圆弧中心点的Y 坐标 |
| **检测圆弧内径** | `float` | float 型，代表检测圆弧的内径 |
| **检测圆弧外径** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.11.2 颜色测量
*手册 PDF 1051–1053 页*

> 颜色测量可测量彩色图像指定区域的颜色信息，包括每个通道的最大值、最小值、均值、标准差和直方图信息。

**参数（1）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **颜色空间** | 支持RGB、HSV、HSI 三种颜色空间，具体介绍请见 术语介绍 章节 | — | 支持RGB、HSV、HSI |

**输出结果（11）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `float int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色。通道*最小值（*代表 1-3）int 型，代表用来存储、传递与颜色相关的图像信息的通道*最小值。通道*最大值（*代表 1-3）int 型，代表用来存储、传递与颜色相关的图像信息的通道*最大值。通道*均值（*代表 1-3）float 型，代表用来存储、传递与颜色相关的图像信息的通道*平均值。通道*标准差（*代表 1-3）float 型，代表用来存储、传递与颜色相关的图像信息的通道*标准差值。通道*直方图（*代表 1-3）int 型，代表将存储、传递与颜色相关的图像信息的通道*用直方图表示的结果 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **ROI 圆弧** | `annull` | annull 型，代表ROI 圆弧 |
| **检测圆弧中心点** | `point` | point 型，代表检测圆弧的中心点 |
| **检测圆弧中心X** | `float` | float 型，代表检测圆弧中心点的X 坐标 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.11.3 颜色转换
*手册 PDF 1054–1057 页*

> 颜色转换可对彩色图像指定区域进行颜色空间转换，并输出该区域转换后图像的指定颜色通道灰度图像。

**参数（3）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **转换类型** | 包含RGB 转灰度、RGB 转HSV、RGB 转HSI、RGB 转YUV。选择不同的转换类型需设置的参数有所差别。选择RGB 转灰度时，需设置转换比例 | — | — |
| **转换比例** |  | — | — |
| **显示通道** |  | — | — |

**输出结果（4）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **输出图像** | `—` |  |
| **输出图像数据** | `binary` | binary 型，代表输出图像的二进制数据 |
| **输出图像宽度** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.11.4 颜色识别
*手册 PDF 1058–1064 页*

> 颜色识别依靠颜色为模板进行分类识别，当不同类物体有着比较明显的颜色差异时颜色识别可实现精准的物体分类并输出相关的分类信息。

**参数（6）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **K 值** | 表示选取前K 个颜色样本中所占数量最多的类作为最佳识别结果，K 值需小于所有标签类中的最小样本数 | — | — |
| **KNN 距离** | 是指不同特征值之间的距离，包含欧式距离、曼哈顿距离、相交距离、偏移距离四种。推荐使用偏移距离 | — | — |
| **欧氏距离** | 最常见的距离变量，是指多维空间里两个点之间的真实距离 | — | — |
| **曼哈顿距离** | 在二维坐标系中两点之间的横坐标和纵坐标的距离总和 | — | — |
| **相交距离** | 由于历史兼容性原因保留，但效果不如偏移距离好 | — | — |
| **偏移距离** | 推荐选择此项，运行效果最优 | — | — |

**输出结果（20）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **置信度** | `float` | float 型，代表识别的字符的置信度。数组，其长度等于字符个数 |
| **最佳匹配名称** | `string` | string 型，代表添加颜色模型，设置每种颜色标签类型，用选取工具提取图像颜色并测量识别指定区域的颜色信息，调整参数设置，输出模板配置，建立完模板后加载图像并设定ROI 限定目标区域，单次执行会输出每个模板对应的识别得分， 识别得分最高的为最佳匹配模型，该模型名称为最佳匹配名称 |
| **最佳分数** | `float` | float 型，代表添加颜色模型，设置每种颜色标签类型，用选取工具提取图像颜色并测量识别指定区域的颜色信息，调整参数设置，输出模板配置，建立完模板后加载图像并设定ROI 限定目标区域，单次执行会输出每个模板对应的识别得分， 识别得分最高的为最佳分数 |
| **类别名** | `string` | string 型，代表创建颜色模型所需要输入的标签类名称，用来区别颜色类 别 |
| **分数** | `float` | float 型，代表匹配到图像的得分，1 表示与模板图像完全相同 |
| **模型H 通道数据** | `float` | float 型，代表将存储、传递与颜色相关的图像色调的模型通道数据 |
| **模型S 通道数据** | `float` | float 型，代表将存储、传递与颜色相关的图像饱和度的模型通道数据 |
| **模型I 通道数据** | `float` | float 型，代表将存储、传递与颜色相关的图像亮度的模型通道数据 |
| **样本H 通道数据** | `float` | float 型，代表将存储、传递与颜色相关的图像色调的样本通道数据 |
| **样本S 通道数据** | `float` | float 型，代表将存储、传递与颜色相关的图像饱和度的样本通道数据 |
| **样本I 通道数据** | `float` | float 型，代表将存储、传递与颜色相关的图像亮度的样本通道数据 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **屏蔽区多边形** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.11.5 颜色分割
*手册 PDF 1065–1069 页*

> 颜色分割从图像中分割出配置的颜色范围，以灰度图和渲染图的方式输出。颜色范围可直接从图像中提取。

**参数（10）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **颜色范围列表** | 呈现已设置的颜色范围，单击可新增。支持1 ~ 16 个颜色范围 | — | — |
| **颜色范围设置** | 可分别对每个颜色范围进行相关设置 | — | — |
| **颜色空间** | 选择设置颜色范围的颜色空间。可选RGB、HSV、HSI。选择不同的颜色空间，需设置的参数有所差别。RGB通过R 通道、G 通道、B 通道分别设置各通道的范围阈值。HSVHSI通过色调、饱和度、亮度分别设置各通道的范围阈值，也可通过左侧示意图的滑块调整范围阈值 | — | 可选RGB、HSV、HSI |
| **自动设定** | 启用该参数时，色品图和明度轴的游标每次根据颜色区域选择框选的区域自动调整为算法推荐的颜色范围；未启用时，框选后游标位置不会自动调整 | — | — |
| **颜色区域选择** | 在模块的输入图像框选指定目标区域，可快速生成对应的三通道范围阈值 | — | — |
| **颜色反转** | 启用该参数时，则选择当前设置颜色范围外的颜色空间作为实际的颜色范围 | — | — |
| **使用独立面积参数** | 未开启时，当前颜色范围使用运行参数中的孔洞填充面积和杂斑筛除面积参数；开启时，可单独设置自己的这两个参数 | — | — |
| **孔洞填充面积** | 面积小于设置数值的孔洞将被填充，与其周边的目标区域合并 | — | — |
| **杂斑筛除面积** | 面积小于设置数值的杂斑将被删除，变为非目标区域或与其周边其他的目标区域合并 | — | — |
| **类别名称** | 可自定义设置对应颜色范围的类别名称 | — | — |

**输出结果（22）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **总面积** | `float` | float 型，代表所有Blob 加起来的总面积 |
| **类别信息** | `image` | image 型，代表类别图信息 |
| **类别名称** | `string` | string 型，代表类别名称，保存在模型中。导入模型时，从模型文件中获取 |
| **灰度值** | `int` | int 型，代表每个类别的灰度值 |
| **输出图像** | `—` |  |
| **输出图像数据** | `binary` | binary 型，代表输出图像的二进制数据 |
| **输出图像宽度** | `int` | int 型，代表输出图像的宽度 |
| **输出图像高度** | `int` | int 型，代表输出图像的高度 |
| **输出图像像素格式** | `int` | int 型，代表输出图像的像素格式。其中17301505 对应Mono8 格式，35127316 对应RGB24 格式 |
| **输出渲染图** | `image` | image 型，代表完成渲染后输出的图像 |
| **输出渲染图** | `image` | image 型，代表完成渲染后输出的图像 |
| **输出渲染图宽度** | `int` | int 型，代表输出渲染图的宽度大小 |
| **输出渲染图高度** | `int` | int 型，代表输出渲染图的高度大小 |
| **输出渲染图像素** | `int` | int 型，代表输出渲染图的像素格式 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.11.6 彩图生成
*手册 PDF 1070–1071 页*

> 彩图生成通过输入3通道Mono8的图，合成彩色图。

**参数（4）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入颜色R** |  | — | — |
| **输入颜色G** |  | — | — |
| **输入颜色B** | 可分别选择作为图像R、G、B 通道分量的灰度图 | — | — |
| **输出图像格式** | 可设置输出图像的格式，可选RGB P3 或RGB C3 | — | 可选RGB P3 或RGB C3 |

**输出结果（3）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **输出图像** | `—` |  |
| **输出图像数据** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)
