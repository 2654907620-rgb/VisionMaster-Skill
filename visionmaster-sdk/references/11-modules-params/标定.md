# 标定 模块参数与结果（VM 用户手册 V4.4.3）

共 10 个模块。来源：官方用户手册第 13 章对应小节。

## 13.8.1 N点标定
*手册 PDF 860–868 页*

> N点标定通过N点的像素坐标和物理坐标进行标定，N需大于等于4。

**参数（31）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **标定点获取** | 可选触发获取和手动输入两种方式。选择触发获取时，还需设置以下参数： | — | 可选触发获取和手动输入两种方式 |
| **标定点输入** | 可选按点、按坐标输入。图像点图像坐标X/Y图像角度图像标定点的位置及角度信息，一般从前序模块订阅 | — | 可选按点、按坐标输入 |
| **物理点** |  | — | — |
| **物理坐标X/Y** |  | — | — |
| **物理角度** | 运动机构的坐标点的位置及角度信息。每个图像点对应一个运动机构的物理点 | — | — |
| **示教** | 启用后，可通过相关设置判断外部输入的信号是否为示教信号 | — | — |
| **外部输入字符** | 可订阅外部输入的字符，例如外部通信的TRIGGER STRING | — | — |
| **外部触发字符** | 判断外部输入字符是否为示教信号的依据。当外部输入字符与该参数内容一致时，则判断该信号为示教信号 | — | — |
| **平移次数** | 设置平移运动的输入点数，只针对X/Y 方向的平移，一般设置为9 | — | — |
| **旋转次数** | 设置旋转运动的输入点数。旋转轴与图像中心不共轴时，需设置该参数，一般设置为3，且旋转是在第5 个点的位置进行 | — | — |
| **点击** | 可在编辑标定点页签查看并编辑各个点对应的图像坐标、物理坐标及角度。标定点获取选择手动输入时，需通过此处自定义或导入标定点的信息 | — | — |
| **自定义** | 双击数据直接修改即可 | — | — |
| **导入** | 可加载已有的点集对信息文件（txt 格式） | — | — |
| **导出** | 可将当前显示的点集对信息导出到本地 | — | — |
| **清空标定点** | 清空当前显示的点集对信息 | — | — |
| **物理坐标系参数** | 可通过设置以下参数自定义物理坐标系 | — | — |
| **基准点X/Y** | 对应图像标定原点的物理点坐标，单位为mm。使用相对坐标时，一般设置为（0,0）；使用绝对坐标时，设置为第5 个点的物理坐标 | — | — |
| **偏移X/Y** | 运动机构每次运动时X/Y 方向的物理偏移量，可正可负，单位为mm。即第5 个点相对第1 个点在X/Y 方向的偏移量 | — | — |
| **移动优先** | 可设置运动机构优先偏移的方向，可选X 优先、Y 优先 | — | 可选X 优先、Y 优先 |
| **换向移动次数** | 可设置运动机构每移动多少次进行换向移动 | — | — |
| **基准角度** | 运动机构旋转的初始角度 | — | — |
| **角度偏移** | 运动机构每次旋转的角度。举例：假设旋转3 次，且旋转角度依次为 -10° → 0° → 10° ，则基准角度为0，角度偏移为10。下图为部分参数设置的示意图，其中平移次数为9，旋转次数为3，基准点X/Y 均为0，偏移量X/Y 均为5，移动优先为X 优先，换向移动为3 | — | — |
| **使用相对坐标** | 可设置运动机构的物理坐标系为相对坐标或绝对坐标。开启时，为相对坐标；未开启时，为绝对坐标。默认关闭。开启后，需配置标定原点参数，设置第几个点为标定原点。一般设置为4。因为物理坐标系从0 开始计数，中间的点为原点 | 默认关闭 | — |
| **标定文件路径** |  | — | — |
| **点击** | 可自定义选择标定文件的存储路径，也可通过订阅的方式加载已有的标定文件或新建标定文件。新建标定文件时，在选择的路径下，自定义输入文件名并单击打开即可，如下图所示 | — | — |
| **更新文件** | 开启该参数后，该模块进行新一轮标定时，会将新的标定结果更新到标定文件路径处选择的标定文件中。否则，标定文件不更新 | — | — |
| **生成标定文件** | 点击生成标定文件按钮可将该模块生成的标定文件另存为 | — | — |
| **相机模式** | 需跟进现场实际情况选择，可选相机静止上相机位（相机固定不动且在被测物上方）、相机静止下相机位（相机固定不动且在被测物下方）、相机运动（相机跟随运动机构运动） | — | 可选相机静止上相机位（相机固定不动且在被测物上方）、相机静止下相机位（相机固定不动且在被测物下方）、相机运动（相机跟随运动机 |
| **自由度** | 可设置拟合homo 矩阵时的自由度。可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换），缩放、旋转、纵横比、倾斜及平移（仿射变换）和缩放、旋转及平移（相似性变换）这三种，自由度逐步降低，推荐使用默认选项。举例：正方形通过透视变换可能会变成梯形，通过仿射变换可能会变成矩形，通过相似性变换虽还是正方形但大小会发生变化 | — | 可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换） |
| **权重函数** | 可选最小二乘、Huber、Tukey 三种算法权重函数，推荐使用默认选项。选择Huber 或Tukey 时，需设置权重系数参数 | — | 可选最小二乘、Huber、Tukey 三种算法权重函数 |
| **权重系数** | 对应选择函数的削波因子，推荐使用默认值 | — | — |

**输出结果（28）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **标定状态** | `int` | int 型，0 代表标定失败，1 代表标定成功 |
| **评估标定误差状态** | `int` | int 型，0 代表误差在正常范围内，1 代表不正常的标定结果 |
| **标定图像点** | `point` | point 型，代表标定图像点。图像坐标Xfloat 型，代表标定时特征点像素坐标X 数值。图像坐标Yfloat 型，代表标定时特征点像素坐标Y 数值 |
| **标定物理点** | `point` | point 型，代表标定物理点 |
| **转换坐标X** | `float` | float 型，代表标定时机构物理坐标X 数值 |
| **转换坐标Y** | `float` | float 型，代表标定时机构物理坐标Y 数值 |
| **平移像素平均误差** | `float` | float 型，代表平移像素过程中，测量值与真实值之间的平均差异。 误差越大，测量结果越不准确 |
| **旋转像素平均误差** | `float` | float 型，代表旋转像素过程中，测量值与真实值之间的平均差异。 误差越大，测量结果越不准确 |
| **旋转轴图像点** | `point` | point 型，代表旋转轴图像中心 |
| **旋转轴图像坐标X** | `float` | float 型，代表旋转中心在图像坐标系中的坐标X |
| **旋转轴图像坐标Y** | `float` | float 型，代表旋转中心在图像坐标系中的坐标Y |
| **旋转中心物理点** | `point` | point 型，代表旋转中心物理点 |
| **旋转中心物理坐标X** | `float` | float 型，代表旋转中心在物理坐标系中的坐标X |
| **旋转中心物理坐标Y** | `float` | float 型，代表旋转中心在物理坐标系中的坐标Y |
| **角度旋转一致性** | `int` | int 型，代表机构物理坐标系角度的变化与像素坐标系角度的变化一致，角度同增或同减 |
| **平移像素最大误差** | `float` | float 型，代表平移像素过程中，测量值与真实值之间的最大差异 |
| **平移像素最大误差对应点数** | `float` | float 型，代表在平移像素过程中，测量值与真实值之间差异最大时，对应点的序号 |
| **平移估计真实误差** | `float` | float 型，代表平移像素完成后，测量值与真实值之间的差异 |
| **平移像素真实最大误差** | `float` | float 型，代表平移像素完成后，测量值与真实值之间的最大差异 |
| **旋转像素最大误差** | `float` | float 型，代表旋转像素过程中，测量值与真实值之间的最大差异 |
| **旋转像素最大误差对应点数** | `int` | int 型，代表旋转像素过程中，测量值与真实值之间最大差异的对应点数 |
| **旋转真实平均误差** | `float` | float 型，代表旋转像素完成后，测量值与真实值之间的平均差异 |
| **旋转真实最大误差** | `float` | float 型，代表旋转像素完成后，测量值与真实值之间的最大差异 |
| **尺度** | `float` | float 型，代表世界坐标系中单位长度对应图像坐标系中的像素数 |
| **x 偏移** | `float` | float 型，代表图像坐标系原点到机构物理坐标系原点的偏移值X，单位为像素 |
| **y 偏移** | `float` | float 型，代表图像坐标系原点到机构物理坐标系原点的偏移值Y，单位为像素 |
| **旋转** | `float` | float 型，代表机构物理坐标系相对于图像坐标系的旋转角度（单位为弧度） |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.10 标定加载
*手册 PDF 903–906 页*

> 标定加载可在订阅的刷新信号非0时重新加载部分标定模块生成的标定文件，并输出相关信息。支持的标定模块为N点标定、映射标定和N图像标定。

**参数（8）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **标定文件** | 需设置标定文件路径、刷新信号和方案存矩阵参数 | — | — |
| **标定文件路径** |  | — | — |
| **点击** | 选择需加载的标定文件 | — | — |
| **刷新信号** | 可自定义或订阅数据，作为标定文件是否刷新的信号。的标定文件是最新的 | — | — |
| **方案存矩阵** | 启用该参数后，可将标定文件数据存入方案中。即使方案未加载标定文件，也能正常运行。图像基准坐标通过X、Y 和R 分别设置图像基准坐标系的原点和角度，供后续模块订阅。可自定义设置，也可从前序模块订阅 | — | — |
| **拍照基准坐标** | 通过X、Y 和R 分别设置拍照基准坐标系的原点和角度，供后续模块订阅。可自定义设置，也可从前序模块订阅 | — | — |
| **示教坐标** | 通过X、Y 和R 分别设置示教坐标系的原点和角度，供后续模块订阅。可自定义设置，也可从前序模块订阅 | — | — |
| **标定信息** | 可查看所加载标定文件的相关信息，如下图所示 | — | — |

**输出结果（33）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **坐标系矩阵** | `float xml` | float 型，代表相对于图像坐标系的坐标系矩阵。13.8.10 标定加载标定加载模块可加载部分模块生成的标定文件（xml 格式），并输出相关信息 |
| **角度旋转一致性** | `int` | int 型，代表机构物理坐标系角度的变化与像素坐标系角度的变化一致，角度同增或同减 |
| **坐标系左右手一致性** | `int` | int 型，1 代表机构物理坐标系和像素坐标系手性一致，-1 代表机构物理坐标系和像素坐标系手性不一致。像素坐标系手性为左手系 |
| **旋转中心图像坐标** | `—` |  |
| **旋转中心图像坐标点** | `—` |  |
| **旋转中心图像坐标X** | `float` | float 型，代表旋转中心在图像坐标系中坐标X |
| **旋转中心图像坐标Y** | `float` | float 型，代表旋转中心在图像坐标系中坐标Y |
| **旋转中心图像坐标R** | `float` | float 型，代表旋转中心在图像坐标系中角度R |
| **旋转中心物理坐标** | `—` |  |
| **旋转中心物理坐标点** | `—` |  |
| **旋转中心物理坐标X** | `float` | float 型，代表旋转中心在物理坐标系中坐标X |
| **旋转中心物理坐标Y** | `float` | float 型，代表旋转中心在物理坐标系中坐标Y |
| **旋转中心物理坐标R** | `float` | float 型，代表旋转中心在物理坐标系中角度R。图像基准坐标图像基准坐标点图像基准坐标Xfloat 型，代表目标在基准位姿时特征点的像素坐标X。图像基准坐标Yfloat 型，代表目标在基准位姿时特征点的像素坐标Y。图像基准坐标Rfloat 型，代表目标在基准位姿时特征边角度R |
| **拍照基准坐标** | `—` |  |
| **拍照基准坐标点** | `—` |  |
| **拍照基准坐标X** | `float` | float 型，代表通常为运动机构N 点标定的第5 点（如果点 位为9 或12 点情况）坐标X |
| **拍照基准坐标Y** | `float` | float 型，代表通常为运动机构N 点标定的第5 点（如果点 位为9 或12 点情况）坐标Y |
| **拍照基准坐标R** | `float` | float 型，代表通常为运动机构N 点标定的第5 点（如果点位为9 或12 点情况）角度R |
| **示教坐标** | `—` |  |
| **示教坐标点** | `—` |  |
| **示教坐标X** | `float` | float 型，代表目标在基准位姿时运动机构去抓取或贴合时的坐标X |
| **示教坐标Y** | `float` | float 型，代表目标在基准位姿时运动机构去抓取或贴合时的坐标Y |
| **示教坐标R** | `float` | float 型，代表目标在基准位姿时运动机构去抓取或贴合时的角度R |
| **目标拍照坐标** | `—` |  |
| **目标拍照坐标点** | `—` |  |
| **目标拍照坐标X** | `float` | float 型，代表映射标定时运动机构在目标拍照位的坐标X |
| **目标拍照坐标Y** | `float` | float 型，代表映射标定时运动机构在目标拍照位的坐标Y |
| **目标拍照坐标R** | `float` | float 型，代表映射标定时运动机构在目标拍照位的角度R |
| **对象拍照坐标** | `—` |  |
| **对象拍照坐标点** | `—` |  |
| **对象拍照坐标X** | `float` | float 型，代表映射标定时运动机构在对象拍照位的坐标X |
| **对象拍照坐标Y** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.2 N图像标定
*手册 PDF 869–874 页*

> N图像标定可通过构建一系列图像点和物理点进行拟合，计算图像坐标系到机械臂世界坐标系之间的关系。

**参数（19）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 可设置该模块的图像数据来源。一般直接订阅图像源模块的的图像 | — | — |
| **标定点输入** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **物理点** |  | — | — |
| **物理坐标X/Y** | 运动机构坐标点的位置信息。每个图像点对应一个运动机构的物理点 | — | — |
| **旋转角度** | 运动机构坐标点的角度信息 | — | — |
| **平移次数** | 设置平移运动的输入点数，只针对X/Y 方向的平移，一般设置为9 | — | — |
| **旋转次数** | 设置旋转运动的输入点数。旋转轴与图像中心不共轴时，需设置该参数，一般设置为3，且旋转是在第5 个点的位置进行 | — | — |
| **物理坐标系参数** | 此部分实现的功能、涉及的参数以及设置方法均与N 点标定中的 物理坐标系参数 一致，此处不再赘述 | — | — |
| **矩阵修正** | 该模块生成的标定文件中，仅平移标定时，默认将第一张图像中棋盘格左上角角点在图像中的点作为图像坐标系的原点；包含旋转标定时，默认将图像旋转中心作为物理坐标系的原点。若该对应关系无法满足要求，可开启该参数自行定义图像原点和物理原点的位置 | — | — |
| **输入方式** | 设置图像点和物理点的获取方式，可选按点、按坐标。图像点图像坐标X/Y可设置图像坐标系原点的位置信息，一般从前序模块订阅 | — | 可选按点、按坐标 |
| **物理点** |  | — | — |
| **物理坐标X/Y** | 可设置运动机构物理坐标原点的位置信息 | — | — |
| **标定文件路径** |  | — | — |
| **更新文件** |  | — | — |
| **生成标定文件** | 以上3 个参数为标定类模块共有参数，功能与操作基本一致，相关介绍参见 N 点标定中相关参数 的介绍 | — | — |
| **清除图像** | 可清除已缓存的参与标定的图像。一般需要重新标定用于清除图像 | — | — |
| **相机移动** | 相机存在相对运动时，则需要启用该参数。否则，无需开启。会影响旋转一致性 | — | — |
| **标定板类型** | 可选海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板。需根据实际使用的标定板类型选择 | — | 可选海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板 |
| **中值滤波状态** | 可设置提取角点前是否执行中值滤波。可选执行滤波和无滤波，推荐使用默认值 | — | 可选执行滤波和无滤波 |

**输出结果（29）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **标定状态** | `int` | int 型，0 代表标定失败，1 代表标定成功 |
| **评估标定误差状态** | `int` | int 型，0 代表误差在正常范围内，1 代表不正常的标定结果 |
| **旋转方向** | `int` | int 型，代表机器臂的旋转方向，可通过调整旋转角度调整现实三维物理坐标系方向 |
| **信息数量** | `int` | int 型，代表收集、存储、传递的信息数量 |
| **标定图像点** | `point` | point 型，代表标定图像点。图像坐标Xfloat 型，代表标定时特征点像素坐标X 数值。图像坐标Yfloat 型，代表标定时特征点像素坐标Y 数值 |
| **平移像素平均误差** | `float` | float 型，代表平移像素过程中，测量值与真实值之间的平均差异。 误差越大，测量结果越不准确 |
| **平移像素最大误差** | `float` | float 型，代表平移像素过程中，测量值与真实值之间的最大差异 |
| **平移像素最大误差对应点数** | `float` | float 型，代表在平移像素过程中，测量值与真实值之间差异最大时，对应点的序号 |
| **平移估计真实误差** | `float` | float 型，代表平移像素完成后，测量值与真实值之间的差异 |
| **平移像素真实最大误差** | `float` | float 型，代表平移像素完成后，测量值与真实值之间的最大差异 |
| **旋转像素平均误差** | `float` | float 型，代表旋转像素过程中，测量值与真实值之间的平均差异。 误差越大，测量结果越不准确 |
| **旋转像素最大误差** | `float` | float 型，代表旋转像素过程中，测量值与真实值之间的最大差异 |
| **旋转像素最大误差对应点数** | `int` | int 型，代表旋转像素过程中，测量值与真实值之间最大差异的对应点数 |
| **旋转真实平均误差** | `float` | float 型，代表旋转像素完成后，测量值与真实值之间的平均差异 |
| **旋转真实最大误差** | `float` | float 型，代表旋转像素完成后，测量值与真实值之间的最大差异 |
| **机构平移最大误差所在图片索引** | `int` | int 型，代表进行平移过程中，测量值与真实值之间的最大差异。指出产生最大差异的图片所在位置 |
| **机构平移图像移动量与机构移动量偏差** | `float` | float 型，代表机构平移过程中，机构平移图像移动量和机构移动量的差异 |
| **机构平移图像尺度变化** | `float` | float 型，代表机构平移过程中图像的尺度变化 |
| **机构平移图像之间旋转变化量** | `float` | float 型，代表机构平移过程中，平移图像的旋转变化值 |
| **机构平移最大误差距离误差** | `int` | int 型，代表机构平移过程中，测量值与真实值之间最大差异的图片距离误差值 |
| **机构平移最大误差图像尺度变化** | `float` | float 型，代表机构平移过程中，测量值与真实值之间最大差异的图片尺度变化 |
| **机构平移最大误差图像之间旋转变化量** | `float` | float 型，代表机构平移完成后，测量值与真实值之间最大差异图片之间的旋转变化量 |
| **机构旋转平均误差** | `float` | float 型，代表机构旋转过程中，计算旋转图像导致计算转化矩阵的平均误差，单位为mm |
| **机构旋转最大误差** | `float` | float 型，代表机构旋转过程中，计算旋转图像导致计算转化矩阵的最大误差，单位为mm |
| **机构旋转最大误差所在图片索引** | `float` | float 型，代表机构旋转过程中，测量值与真实值之间最大差异的图片所在位置 |
| **旋转中心坐标** | `—` |  |
| **旋转中心坐标X** | `float` | float 型，代表通过旋转图像计算出的旋转中心图像坐标X |
| **旋转中心坐标Y** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.3 标定板标定
*手册 PDF 875–881 页*

> 标定板标定通过各种类型的标定板进行标定，标定板类型分为棋盘格、圆、海康I型、海康II型。

**参数（18）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **标定文件路径** |  | — | — |
| **更新文件** |  | — | — |
| **生成标定文件** | 以上3 个参数为标定类模块共有参数，功能与操作基本一致，相关介绍参见 N 点标定中相关参数 的介绍 | — | — |
| **原点X** |  | — | — |
| **原点Y** | 可设置默认物理坐标系原点进行X/Y 方向的平移，单位为mm。该参数数值为负数时，以X/Y 轴反方向平移；该参数数值为正数时，以X/Y 轴正方向平移 | — | — |
| **旋转角度** | 可设置物理坐标系的旋转方向和角度。该参数数值为负数时，以逆时针方向旋转对应的角度；该参数数值为正数时，以顺时针方向旋转对应的角度 | — | — |
| **坐标系模式** | 可设置物理坐标系的类型，可选左手坐标系和右手坐标系。两种坐标系如下图所示 | — | 可选左手坐标系和右手坐标系 |
| **物理尺寸** | 对应棋盘格各个格子的边长或圆点阵中相邻圆的圆心距，单位为mm | — | — |
| **标定板类型** | 可选棋盘格标定板、圆标定板、海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板。各类型标定板的介绍请参见上文的 模块原理 。选择不同类型的标定板，部分需设置的参数有所差异 | — | 可选棋盘格标定板、圆标定板、海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板 |
| **选择除圆标定板外的标定板类型时** | 需设置中值滤波状态参数 | — | — |
| **中值滤波状态** | 可设置提取角点前是否执行中值滤波。可选执行滤波和无滤波，推荐使用默认设置 | — | 可选执行滤波和无滤波 |
| **选择圆标定板时** | 需设置点圆度、边缘提取阈值和圆点类型参数 | — | — |
| **点圆度** | 可设置圆形度的最小值。识别到的区域圆形度低于设置的数值时，则认为不是有效的圆形区域 | — | — |
| **边缘提取阈值** | 可设置圆边缘两侧的灰度差有效范围。当灰度差不在该范围内时，则认为不是有效圆形区域 | — | — |
| **圆点类型** | 可设置识别的圆点类型，可选白底黑圆和黑底白圆 | — | 可选白底黑圆和黑底白圆 |
| **自由度** | 可设置拟合homo 矩阵时的自由度。可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换），缩放、旋转、纵横比、倾斜及平移（仿射变换）和缩放、旋转及平移（相似性变换）这三种，自由度逐步降低，推荐使用默认选项。举例：正方形通过透视变换可能会变成梯形，通过仿射变换可能会变成矩形，通过相似性变换虽还是正方形但大小会发生变化 | — | 可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换） |
| **权重函数** | 可选最小二乘法、Huber、Tukey 三种算法权重函数，推荐使用默认选项。选择Huber 或Tukey 时，需设置权重系数参数 | — | 可选最小二乘法、Huber、Tukey 三种算法权重函数 |
| **权重系数** | 对应选择函数的削波因子，推荐使用默认值 | — | — |

**输出结果（24）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **标定误差** | `float` | float 型，代表标定过程中，标定测量值与真实值间的差异。 标定误差越大，测量结果越不准确。相机标定允许误差范围在0.1~0.5 之间 |
| **尺度** | `float` | float 型，代表世界坐标系中单位长度对应图像坐标系中的像素数 |
| **标定点** | `point` | point 型，代表标定板角点像素 |
| **标定点X** | `float` | float 型，代表标定板角点像素坐标X |
| **标定点Y** | `float` | float 型，代表标定板角点像素坐标Y |
| **标定原点** | `—` |  |
| **标定原点X** | `float` | float 型，代表标定板左上角第一个角点坐标X |
| **标定原点Y** | `float` | float 型，代表标定板左上角第一个角点坐标Y |
| **坐标X 向量** | `—` |  |
| **坐标X 向量X** | `float` | float 型，代表标定板物理坐标系X 轴单位向量的X 值 |
| **坐标X 向量Y** | `float` | float 型，代表标定板物理坐标系X 轴单位向量的Y 值 |
| **坐标Y 向量** | `—` |  |
| **坐标Y 向量X** | `float` | float 型，代表标定板物理坐标系Y 轴单位向量的X 值 |
| **坐标Y 向量Y** | `float` | float 型，代表标定板物理坐标系Y 轴单位向量的Y 值 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |
| **检测区域中心点X** | `float` | float 型，代表检测区域中心点的X 坐标 |
| **检测区域中心点Y** | `float` | float 型，代表检测区域中心点的Y 坐标 |
| **检测区域宽度** | `float` | float 型，代表检测区域的宽度 |
| **检测区域高度** | `float` | float 型，代表检测区域的高度 |
| **检测区域角度** | `float` | float 型，代表检测区域相对于水平方向（正准位姿）的旋转角度。顺时针旋转，角度为正；逆时针旋转，角度为负。若检测区域处于正准位姿，则为0 度 |
| **标定点数** | `int` | int 型，代表所提取标定板上的特征点数量 |
| **平移X** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.4 相机映射
*手册 PDF 882–884 页*

> 相机映射可通过两个相机的对应像素点对，标定出对象点所在坐标系到目标点所在坐标系的转换关系，输出标定文件、标定状态和标定误差。

**参数（9）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入方式** | 可设置示教点*-运行点*的点信息输入方式，可选按点、按坐标。选择按点时，自定义或订阅的数据为点的坐标；选择按坐标时，自定义或订阅的数据为点X 和点Y，从而组成点的坐标 | — | 可选按点、按坐标 |
| **示教点*-运行点*** | 可设置两个图像坐标系下点与点的映射。至少需设置两组映射关系，也可通过 | — | — |
| **增加映** | 射关系 | — | — |
| **标定文件路径** |  | — | — |
| **更新文件** |  | — | — |
| **生成标定文件** | 以上3 个参数为标定类模块共有参数，功能与操作基本一致，相关介绍参见 N 点标定中相关参数 的介绍 | — | — |
| **自由度** | 可设置拟合homo 矩阵时的自由度。可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换），缩放、旋转、纵横比、倾斜及平移（仿射变换）和缩放、旋转及平移（相似性变换）这三种，自由度逐步降低，推荐使用默认选项。举例：正方形通过透视变换可能会变成梯形，通过仿射变换可能会变成矩形，通过相似性变换虽还是正方形但大小会发生变化 | — | 可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换） |
| **权重函数** | 可选最小二乘法、Huber、Tukey 三种算法权重函数，推荐使用默认选项。选择Huber 或Tukey 时，需设置权重系数参数 | — | 可选最小二乘法、Huber、Tukey 三种算法权重函数 |
| **权重系数** | 对应选择函数的削波因子，推荐使用默认值 | — | — |

**输出结果（6）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **x 方向比例** | `float` | float 型，代表运行点所在坐标系到示教点所在坐标系的X 坐标比例 |
| **y 方向比例** | `float` | float 型，代表运行点所在坐标系到示教点所在坐标系的Y 坐标比例 |
| **标定状态** | `int` | int 型，0 代表标定失败，1 代表标定成功 |
| **平移X** | `float` | float 型，代表利用计算得到的标定矩阵，将世界坐标系原点映射到图像坐标系得到的坐标X |
| **平移Y** | `float` | float 型，代表利用计算得到的标定矩阵，将世界坐标系原点映射到图像坐标系得到的坐标Y |
| **旋转** | `float` | float 型，代表机构物理坐标系相对于图像坐标系的旋转角度（单位为弧度） |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.5 映射标定
*手册 PDF 885–888 页*

> 映射标定主要用于不同标定坐标系下的坐标相互转换，仅支持使用自研标定板。

**参数（22）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入方式** | 选择模块所需信息的输入方式，可选图像输入或文件输入。图像输入选择图像输入时，需通过前序模块获取带海康自研标定板的图像 | — | 可选图像输入或文件输入 |
| **输入源1/2** | 分别选择2 张前序模块输出的图像 | — | — |
| **形状** | 可选择全屏或绘制矩形ROI，相关操作参见 绘制ROI 区域 | — | 可选择全屏或绘制矩形ROI |
| **物理点输入** | 设置物理点的输入方式，可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **物理点** |  | — | — |
| **物理坐标X/Y** |  | — | — |
| **物理角度** | 运动机构坐标点的位置及角度信息。每个图像点对应一个运动机构的物理点 | — | — |
| **示教** | 启用后，可通过相关设置判断外部输入的信号是否为示教信号 | — | — |
| **外部输入字符** | 可订阅外部输入的字符，例如外部通信的TRIGGER STRING | — | — |
| **外部触发字符** | 判断外部输入字符是否为示教信号的依据。当外部输入字符与该参数内容一致时，则判断该信号为示教信号 | — | — |
| **文件输入** | 选择文件输入时，需通过前序模块获取海康自研标定板的标定文件 | — | — |
| **标定文件1/2** |  | — | — |
| **点击** | 选择海康自研标定板图像对应的标定文件 | — | — |
| **刷新信号** | 可自定义或订阅数据，作为标定文件是否刷新的信号。新。读取的标定文件是最新的 | — | — |
| **标定文件路径** |  | — | — |
| **更新文件** |  | — | — |
| **生成标定文件** | 以上3 个参数为标定类模块共有参数，功能与操作基本一致，相关介绍参见 N 点标定中相关参数 的介绍 | — | — |
| **标定板类型** | 可选海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板。需根据实际使用的标定板类型选择 | — | 可选海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板 |
| **自由度** | 可设置拟合homo 矩阵时的自由度。可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换），缩放、旋转、纵横比、倾斜及平移（仿射变换）和缩放、旋转及平移（相似性变换）这三种，自由度逐步降低，推荐使用默认选项。举例：正方形通过透视变换可能会变成梯形，通过仿射变换可能会变成矩形，通过相似性变换虽还是正方形但大小会发生变化 | — | 可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换） |
| **权重函数** | 可选最小二乘、Huber、Tukey 三种算法权重函数，推荐使用默认选项。选择Huber 或Tukey 时，需设置权重系数参数 | — | 可选最小二乘、Huber、Tukey 三种算法权重函数 |
| **权重系数** | 对应选择函数的削波因子，推荐使用默认值 | — | — |
| **中值滤波状态** | 需分别对输入源1 和2 进行设置。可设置提取角点前是否执行中值滤波。可选执行滤波和无滤波，推荐使用默认值 | — | 可选执行滤波和无滤波 |

**输出结果（15）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **标定状态** | `int` | int 型，0 代表标定失败，1 代表标定成功 |
| **X 方向比例** | `float` | float 型，代表运行点所在坐标系到示教点所在坐标系的X 坐标比例 |
| **Y 方向比例** | `float` | float 型，代表运行点所在坐标系到示教点所在坐标系的Y 坐标比例 |
| **映射误差** | `float` | float 型，代表进行映射过程中，映射测量值与真实值之间的差异。 映射误差越大，测量结果越不准确 |
| **当前角点** | `—` |  |
| **当前角点X 坐标** | `float` | float 型，代表第一幅标定板图像中提取的角点坐标X 数值 |
| **当前角点Y 坐标** | `float` | float 型，代表第一幅标定板图像中提取的角点坐标Y 数值 |
| **目标角点** | `—` |  |
| **目标角点X 坐标** | `float` | float 型，代表第二幅标定板图像中提取的角点坐标X 数值 |
| **目标角点Y 坐标** | `float` | float 型，代表第二幅标定板图像中提取的角点坐标Y 数值 |
| **映射角点** | `—` |  |
| **映射角点X 坐标** | `float` | float 型，代表第一幅图中角点映射到第二幅图像中的X 坐标 |
| **映射角点Y 坐标** | `float` | float 型，代表第一幅图中角点映射到第二幅图像中的Y 坐标 |
| **当前角点数** | `—` |  |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.6 畸变标定
*手册 PDF 889–893 页*

> 畸变标定可对存在畸变的标定板图像进行标定。

**参数（17）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 选择前序模块输出的带畸变的标定板图像 | — | — |
| **形状** | 可选择全屏或绘制矩形ROI，相关操作参见 绘制ROI 区域 | — | 可选择全屏或绘制矩形ROI |
| **标定文件路径** |  | — | — |
| **更新文件** |  | — | — |
| **生成标定文件** | 以上3 个参数为标定类模块共有参数，功能与操作基本一致，相关介绍参见 N 点标定中相关参数 的介绍 | — | — |
| **校正点输入** | 可选按点、按坐标输入 | — | 可选按点、按坐标输入 |
| **校正中心X/Y** |  | — | — |
| **校正中心点** | 设置畸变校正的中心点 | — | — |
| **畸变类型** | 可选透视畸变、径向畸变和径向透视畸变，根据实际情况选择即可 | — | 可选透视畸变、径向畸变和径向透视畸变 |
| **矫正评估使能** | 开启该功能后，模块会矫正标定图像。图像上畸变的地方会被尽量处理平直以便降低标定时产生的误差。同时模块会额外输出一张矫正后的图像 | — | — |
| **矫正后标定点** | 开启矫正后，图像上标定点的位置相比原图会有位移。若需要查看矫正后图像所对应每个标定点的位置和误差系数，需开启该参数 | — | — |
| **评估误差阈值** | 每个标定点有一个对应的评估误差，若误差值超过该参数设置的数值，则此标定点在图片上会显示红色，不超过则显示绿色 | — | — |
| **标定板类型** | 可选棋盘格标定板、圆标定板、海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板，根据实际情况选择即可 | — | 可选棋盘格标定板、圆标定板、海康I 型标定板、海康II 型标定板、海康扩展I 型标定板、海康扩展II 型标定板 |
| **自由度** | 可设置拟合homo 矩阵时的自由度。可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换），缩放、旋转、纵横比、倾斜及平移（仿射变换）和缩放、旋转及平移（相似性变换）这三种，自由度逐步降低，推荐使用默认选项。举例：正方形通过透视变换可能会变成梯形，通过仿射变换可能会变成矩形，通过相似性变换虽还是正方形但大小会发生变化 | — | 可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换） |
| **中值滤波状态** | 可设置提取角点前是否执行中值滤波。可选执行滤波和无滤波，推荐使用默认值 | — | 可选执行滤波和无滤波 |
| **权重函数** | 可选最小二乘、Huber、Tukey 三种算法权重函数，推荐使用默认选项。选择Huber 或Tukey 时，需设置权重系数参数 | — | 可选最小二乘、Huber、Tukey 三种算法权重函数 |
| **权重系数** | 对应选择函数的削波因子，推荐使用默认值 | — | — |

**输出结果（11）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **标定误差** | `float` | float 型，代表标定过程中，标定测量值与真实值间的差异。 标定误差越大，测量结果越不准确。相机标定允许误差范围在0.1~0.5 之间 |
| **标定点数** | `int` | int 型，代表所提取标定板上的特征点数量 |
| **标定点** | `point` | point 型，代表标定板角点像素 |
| **标定点X** | `float` | float 型，代表标定板角点像素坐标X |
| **标定点Y** | `float` | float 型，代表标定板角点像素坐标Y |
| **标定点状态** | `float` | float 数组型，由“评估误差阈值”和“标定点评估误差”运行参数之间的大小决定。误差大于阈值时取0，输出图片上对应点显示红色；误差小于等于阈值时取1，输出图片上对应点显示绿色 |
| **评估误差** | `float` | float 型，代表总体评估误差。如需查看该参数，需提前开启矫正评估功能 |
| **标定点评估误差** | `float` | float 型，代表标定点的评估误差。如需查看该参数，需提前开启矫正评估功能 |
| **检测区域** | `box` | box 型，代表图像中需进行检测的ROI 区域 |
| **检测区域中心点** | `point` | point 型，代表检测区域的中心点 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.7 平移旋转标定
*手册 PDF 894–898 页*

> 平移旋转标定模块基于图像特征点实现平移旋转标定，输出标定文件，旋转中心像素空间坐标与物理空间坐标。

**参数（16）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **标定类型** | 可选平移标定、平移旋转标定。选择平移旋转标定时，还需设置以下参数： | — | 可选平移标定、平移旋转标定 |
| **旋转次数** | 设置旋转运动的输入点数。旋转轴与图像中心不共轴时，需设置该参数，一般设置为3，且旋转是在第5 点的位置进行 | — | — |
| **组合标定** | 开启该参数后，该模块支持平移并旋转后，再平移回视野的标定方式。该方式对平移精度要求较高。若小视野场景中机构只能小角度旋转，从而导致旋转中心拟合不准确。此时可启用该参数控制机构旋转较大角度再平移回视野的方式进行旋转中心标定 | — | 支持平移并旋转后 |
| **标定点获取** | 可选触发获取、手动输入。一般选择触发获取。选择手动输入时，该模块可单独运行，即手动输入坐标点生成标定矩阵。选择触发获取时，还需设置以下参数： | — | 可选触发获取、手动输入 |
| **标定点输入** | 图像坐标X/Y图像点图像角度 | — | — |
| **物理坐标X/Y** |  | — | — |
| **物理点** |  | — | — |
| **物理角度** |  | — | — |
| **示教** | 以上参数与N 点标定模块相同，功能与操作一致，相关介绍参见 N 点标定中相关参数的介绍 | — | — |
| **相机模式** | 可选相机运动、相机静止，根据实际情况选择即可 | — | 可选相机运动、相机静止 |
| **自由度** | 可设置拟合homo 矩阵时的自由度。可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换），缩放、旋转、纵横比、倾斜及平移（仿射变换）和缩放、旋转及平移（相似性变换）这三种，自由度逐步降低，推荐使用默认选项。举例：正方形通过透视变换可能会变成梯形，通过仿射变换可能会变成矩形，通过相似性变换虽还是正方形但大小会发生变化 | — | 可选缩放、旋转、纵横比、倾斜、平移及透射（透视变换） |
| **平移次数** | 设置平移运动的输入点数，只针对X/Y 方向的平移，一般设置为9 | — | — |
| **标定文件路径** |  | — | — |
| **更新文件** |  | — | — |
| **生成标定文件** | 以上3 个参数为标定类模块共有参数，功能与操作基本一致，相关介绍参见 N 点标定中相关参数 的介绍 | — | — |
| **清空标定点** | 可将标定点坐标全部重置为0 | — | — |

**输出结果（28）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **标定状态** | `int` | int 型，0 代表标定失败，1 代表标定成功 |
| **坐标系左右手一致性** | `int` | int 型，1 代表机构物理坐标系和像素坐标系手性一致，-1 代表机构物理坐标系和像素坐标系手性不一致。像素坐标系手性为左手系 |
| **标定图像点** | `point` | point 型，代表标定图像点。图像坐标Xfloat 型，代表标定时特征点像素坐标X 数值。图像坐标Yfloat 型，代表标定时特征点像素坐标Y 数值 |
| **标定物理点** | `point` | point 型，代表标定物理点 |
| **转换坐标X** | `float` | float 型，代表标定时机构物理坐标X 数值 |
| **转换坐标Y** | `float` | float 型，代表标定时机构物理坐标Y 数值 |
| **平移像素平均误差** | `float` | float 型，代表平移像素过程中，测量值与真实值之间的平均差异。 误差越大，测量结果越不准确 |
| **旋转像素平均误差** | `float` | float 型，代表旋转像素过程中，测量值与真实值之间的平均差异。 误差越大，测量结果越不准确 |
| **旋转轴图像点** | `point` | point 型，代表旋转轴图像中心 |
| **旋转轴图像坐标X** | `float` | float 型，代表旋转中心在图像坐标系中的坐标X |
| **旋转轴图像坐标Y** | `float` | float 型，代表旋转中心在图像坐标系中的坐标Y |
| **旋转中心物理点** | `point` | point 型，代表旋转中心物理点 |
| **旋转中心物理坐标X** | `float` | float 型，代表旋转中心在物理坐标系中的坐标X |
| **旋转中心物理坐标Y** | `float` | float 型，代表旋转中心在物理坐标系中的坐标Y |
| **角度旋转一致性** | `int` | int 型，代表机构物理坐标系角度的变化与像素坐标系角度的变化一致，角度同增或同减 |
| **平移像素最大误差** | `float` | float 型，代表平移像素过程中，测量值与真实值之间的最大差异 |
| **平移像素最大误差对应点数** | `float` | float 型，代表在平移像素过程中，测量值与真实值之间差异最大时，对应点的序号 |
| **平移估计真实误差** | `float` | float 型，代表平移像素完成后，测量值与真实值之间的差异 |
| **平移像素真实最大误差** | `float` | float 型，代表平移像素完成后，测量值与真实值之间的最大差异 |
| **旋转像素最大误差** | `float` | float 型，代表旋转像素过程中，测量值与真实值之间的最大差异 |
| **旋转像素最大误差对应点数** | `int` | int 型，代表旋转像素过程中，测量值与真实值之间最大差异的对应点数 |
| **旋转真实平均误差** | `float` | float 型，代表旋转像素完成后，测量值与真实值之间的平均差异 |
| **旋转真实最大误差** | `float` | float 型，代表旋转像素完成后，测量值与真实值之间的最大差异 |
| **尺度** | `float` | float 型，代表世界坐标系中单位长度对应图像坐标系中的像素数 |
| **x 偏移** | `float` | float 型，代表图像坐标系原点到机构物理坐标系原点的偏移值X，单位为像素 |
| **y 偏移** | `float` | float 型，代表图像坐标系原点到机构物理坐标系原点的偏移值Y，单位为像素 |
| **旋转** | `float` | float 型，代表机构物理坐标系相对于图像坐标系的旋转角度（单位为弧度） |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.8 旋转标定
*手册 PDF 899–900 页*

> 旋转标定模块基于图像特征点实现旋转中心标定，输出旋转中心像素空间坐标与物理空间坐标。

**参数（8）**

| 参数名 | 说明 | 默认值 | 取值范围/枚举 |
|---|---|---|---|
| **输入源** | 选择前序模块输出的需标定图像 | — | — |
| **输入方式** | 可选按点、按坐标输入方式明确图像点的来源。图像坐标X/Y图像点图像标定点的位置信息，一般从前序模块订阅 | — | 可选按点、按坐标输入方式明确图像点的来源 |
| **物理旋转角度** | 运动机构每次旋转的物理角度 | — | — |
| **旋转次数** | 设置旋转运动的输入点数。旋转轴与图像中心不共轴时，需设置该参数，一般设置为3，且旋转是在第5 个点的位置进行 | — | — |
| **反向纠正** | 旋转半径很大或物理旋转角度很小时，若拟合得到的旋转中心坐标与实际偏差较大，可启用该参数重新进行旋转标定 | — | — |
| **加载标定文件** |  | — | — |
| **点击** | 可自定义选择或新建标定文件，也可通过订阅的方式加载已有的标定文件或新建标定文件 | — | — |
| **刷新信号** | 可自定义或订阅数据，作为标定文件是否刷新的信号。标定文件是最新的 | — | — |

**输出结果（3）**

| 结果名 | 类型 | 说明 |
|---|---|---|
| **模块状态** | `int` | int 型，0 代表NG，此时模块呈现红色；1 代表OK，此时模块呈现绿色 |
| **剩余标定次数** | `int` | int 型，代表标定流程距离标定完成剩余的标定次数 |
| **旋转轴图像点** | `point` | point 型，代表旋转轴图像中心 |

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)

## 13.8.9 坐标系
*手册 PDF 901–902 页*

> 坐标系可通过手动绘制或订阅的方式创建坐标系。

[← 返回总索引](00-INDEX.md) · [→ 模块职责速览](../04-modules-catalog.md)
